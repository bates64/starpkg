on:
  push:
    tags:
      - 'v*'

name: Release

# See https://github.com/actions/create-release/issues/14

jobs:
  publish:
    name: Publish on crates.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cargo publish
        uses: actions-rs/cargo@v1
        with:
          command: publish
          args: --token ${{ secrets.CRATES_IO_TOKEN }} # MMgFYhVHa8BrlcQQrRqbxbYtdVoULnmo

  release:
    name: Make GitHub release
    runs-on: ubuntu-latest
    steps:
      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: ${{ github.ref }}
          body: ""
          draft: false
          prerelease: false # See https://github.com/actions/create-release/issues/9

      - name: Output release_url.txt
        run: echo "${{ steps.create_release.outputs.upload_url }}" > release_url.txt

      - name: Upload release_url.txt artifact for build jobs
        uses: actions/upload-artifact@v1
        with:
          name: release_url
          path: release_url.txt

  build_unix:
    name: Build (Linux/MacOS)
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Download release_url.txt artifact from release job
        uses: actions/download-artifact@v1
        with:
          name: release_url

      - name: Read release_url.txt
        id: release_info
        shell: bash
        run: |
          value=`cat release_url/release_url.txt`
          echo ::set-output name=upload_url::$value

      - name: Install stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cargo build
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release

      - name: Zip release binary
        run: |
          zip starpkg-unix.zip -9 target/release/starpkg

      - name: Upload zip asset to release
        id: upload-release-asset
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.release_info.outputs.upload_url }}
          asset_path: ./starpkg-unix.zip
          asset_name: starpkg-unix.zip
          asset_content_type: application/zip

  build_windows:
    name: Build (Windows)
    needs: release
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Download release_url.txt artifact from release job
        uses: actions/download-artifact@v1
        with:
          name: release_url

      - name: Read release_url.txt
        id: release_info
        shell: bash
        run: |
          value=`cat release_url/release_url.txt`
          echo ::set-output name=upload_url::$value

      - name: Install stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cargo build
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release

      - name: Zip release binary
        run: |
          zip starpkg-windows.zip -9 target/release/starpkg.exe

      - name: Upload zip asset to release
        id: upload-release-asset
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.release_info.outputs.upload_url }}
          asset_path: ./starpkg-windows.zip
          asset_name: starpkg-windows.zip
          asset_content_type: application/zip
